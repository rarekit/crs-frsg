{% extends 'CrousBackendBundle::layout.html.twig' %} 

{% block body %}
<div class="row" style="padding: 5px;">
    {% include 'CrousBackendBundle:Block:messages.html.twig' %}
    
    <h3>{{'Online Service Management'|trans}}</h3>
    <div class="btn-toolbar pull-right" style="margin-bottom: 9px">
        <a class="btn btn-default" href="{{path('crous_backend_onlineservice_add')}}" role="button">{{'Add New'|trans}}</a>     
    </div>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>{{'Title'|trans}}</th>
                <th>{{'Region'|trans}}</th>
                <th width="20%">{{'Action'|trans}}</th>
            </tr>
        </thead>
        <tbody>
            {% for service in entities%}
            <tr>
                <td>{{service.title}}</td>
                <td>{{service.region.name}}</td>
                <td>
                    <a href="{{path('crous_backend_onlineservice_add', {id: service.id})}}">{{'Edit'|trans}}</a> |
                    <a onclick="return confirm('{{'Do you want to delete this record?'|trans}}');" href="{{path('crous_backend_onlineservice_delete', {id: service.id, token: token})}}">{{'Delete'|trans}}</a>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {{pagination|raw}}
</div>
{% endblock %}

