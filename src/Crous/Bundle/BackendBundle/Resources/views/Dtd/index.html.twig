{% extends 'CrousBackendBundle::layout.html.twig' %} 

{% block body %}
<div class="row" style="padding: 5px;">
    {% include 'CrousBackendBundle:Block:messages.html.twig' %}
    
    <h3>{{'DTD Management'|trans}}</h3>
    <div class="btn-toolbar pull-right" style="margin-bottom: 9px">
        <a class="btn btn-default" href="{{path('crous_backend_dtd_add')}}" role="button">{{'Add New'|trans}}</a>     
    </div>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th width="1%">{{'#'|trans}}</th>
                <th>{{'Label'|trans}}</th>
                <th>{{'File'|trans}}</th>
                <th>{{'Region'|trans}}</th>
                <th width="20%">{{'Action'|trans}}</th>
            </tr>
        </thead>
        <tbody>
            {% for dtd in entities%}
            <tr>
                <td>{{loop.index}}</td>
                <td>{{dtd.name}}</td>
                <td>{{dtd.filename}}</td>
                <td>{{dtd.dtd}}</td>
                <td>
                    <a href="{{path('crous_backend_dtd_add', {id: dtd.id})}}">{{'Edit'|trans}}</a> |
                    <a onclick="return confirm('{{'Do you want to delete this record?'|trans}}');" href="{{path('crous_backend_dtd_delete', {id: dtd.id, token: token})}}">{{'Delete'|trans}}</a>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {{pagination|raw}}
</div>
{% endblock %}

